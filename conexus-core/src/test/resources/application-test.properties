# Test configuration for Conexus

# Logging configuration for tests
logging.level.com.boredhf.conexus=DEBUG
logging.level.redis.embedded=WARN
logging.level.redis.clients.jedis=WARN

# Test Redis configuration
conexus.test.redis.timeout=5000
conexus.test.redis.startup-timeout=10000

# Test timeouts
conexus.test.default-timeout=5000
conexus.test.integration-timeout=30000

# Test server configurations
conexus.test.server1.id=server-1
conexus.test.server2.id=server-2